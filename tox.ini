[tox]
envlist = py311  #Specify Python version
skipsdist = True

[testenv]
deps =
    coverage  # Assuming you want coverage reporting

# Replace pytest commands with unittest commands
commands =
    coverage run -m unittest discover
    coverage report -m

[coverage:run]
relative_files = True
source = my_project/
branch = True
